% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/segment_range_filter.R
\name{trajectory_range}
\alias{trajectory_range}
\alias{read_trajectory}
\alias{add_range}
\alias{filter_range}
\title{Trajectory-based range filtering utilities}
\usage{
read_trajectory(file, dt = 0.5)

add_range(las, traj)

filter_range(las, distance = 15, hag_max = 4)
}
\arguments{
\item{file}{Character. Path to the ASCII trajectory file}

\item{dt}{Numeric. Keep a record every \code{dt} seconds. Default 0.5.}

\item{las}{A \code{LAS} object.}

\item{traj}{An \code{sf} trajectory object as returned by \link{read_trajectory}.}

\item{distance}{Numeric. Maximum allowed distance to the sensor.}

\item{hag_max}{Numeric. Maximum allowed height above ground.}
}
\value{
An \code{sf} object with point geometry and a \code{gpstime} attribute.

A \code{LAS} object with an additional attribute \code{range}.

A filtered \code{LAS} object.
}
\description{
These functions implement a simple workflow to:
\enumerate{
  \item read trajectory file,
  \item compute point-to-sensor range using the trajectory,
  \item filter points based on range and height above ground.
}
They are designed to be used together, but can also be called independently.

\code{read_trajectory}: Reads a trajectory file and converts it to an \code{sf} point object.
The first column is assumed to be the GPS time and is renamed to \code{gpstime}.
The file must contain columns named \code{x}, \code{y}, and \code{z}.

\code{add_range}: Computes the distance between each point and the sensor position
derived from a trajectory, and stores it as a new LAS attribute named
\code{range}.

\code{filter_range}: Filters a point cloud by removing points that are too far to the sensor
and below a given height above ground. For example, if \code{distance = 15}
and \code{hag_max = 4}, points that are beyond 15 m of the sensor \emph{and}
lower than 4 m above ground are removed. Points farther than 15 m from the
sensor but higher than 4 m above ground, are retained. This prevents the
removal of canopy point.
}
