% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/qsf_filter.R
\name{qsf_filter}
\alias{qsf_filter}
\alias{qsf_filter_ok}
\alias{qsf_filter_sapling}
\alias{qsf_filter_nomeasure}
\alias{qsf_filter_broken}
\alias{qsf_filter_flagged}
\alias{qsf_filter_warnings}
\alias{qsf_filter_errors}
\alias{qsf_filter_messages}
\title{Filter a qsf object}
\usage{
qsf_filter(
  qsf,
  code = NULL,
  type = NULL,
  invert = FALSE,
  flag = NULL,
  dbh = NULL,
  height = NULL,
  ...
)

qsf_filter_ok(qsf, ...)

qsf_filter_sapling(qsf, ...)

qsf_filter_nomeasure(qsf, ...)

qsf_filter_broken(qsf, ...)

qsf_filter_flagged(qsf, code = NULL, type = NULL, ...)

qsf_filter_warnings(qsf, code = NULL, ...)

qsf_filter_errors(qsf, code = NULL, ...)

qsf_filter_messages(qsf, code = NULL, ...)
}
\arguments{
\item{qsf}{A \code{qsf} object.}

\item{code}{character. One or more \link{qsf_log} codes (e.g. \code{c("W0", "E1")}).
By default, trees carrying any of these codes are \strong{kept}. Use \code{invert = TRUE}
to instead \strong{exclude} trees carrying these codes. In \code{qsf_filter_ok()},
\code{qsf_filter_warnings()}, \code{qsf_filter_errors()} and \code{qsf_filter_messages()},
\code{code} narrows the match to specific codes within that function's type;
left \code{NULL} (default) it matches any code of that type.}

\item{type}{character. One or more log types: \code{"warning"}, \code{"message"}, \code{"error"}.
Same semantics as \code{code}, and combined with \code{code} (if both given, an entry
must match both to be considered flagged). Only exposed on \code{qsf_filter()}
and \code{qsf_filter_flagged()}; the other wrappers fix \code{type} for you.}

\item{invert}{logical. If \code{FALSE} (default), trees matching \code{code}/\code{type} are
kept. If \code{TRUE}, matching trees are dropped. Only exposed on \code{qsf_filter()}.}

\item{flag}{logical. Filters on the mere \emph{presence} of a log entry, regardless
of its code or type (independent of \code{code}/\code{type}, combined with them by AND
like every other filter here). \code{FALSE} keeps only trees with \strong{no log entry
at all} (clean/"good" QSMs). \code{TRUE} keeps only trees with \strong{at least one}
log entry, of any code or type. Default \code{NULL}: no filtering on this criterion.
This is what you want when you need "everything with nothing flagged" without
having to enumerate every possible \code{type}. Only exposed on \code{qsf_filter()}.}

\item{dbh}{numeric(2). Keep only trees with a DBH (see \link{qsm_dbh}) inside
\code{c(min, max)}, in meters.}

\item{height}{numeric(2). Keep only trees with a height (see \link{qsm_height})
inside \code{c(min, max)}, in meters.}

\item{...}{In \code{qsf_filter()}: unused, reserved for additional filters in the
future. In \verb{qsf_filter_*()} wrappers: passed on to \code{qsf_filter()} (e.g.
\code{dbh = c(0.2, Inf)}, \code{height = c(0, 30)}), so a wrapper can be chained
with the other filters in a single call.}
}
\value{
A \code{qsf} object containing only the selected QSMs.
}
\description{
\code{qsf_filter()} subsets a \code{qsf} object, retaining only the trees that
satisfy the requested criteria. Filters compose with AND semantics: a tree
is retained only if it satisfies every criterion supplied.\cr\cr
A handful of \verb{qsf_filter_*()} wrappers cover the most common triage needs
on top of \code{qsf_filter()}: pulling out clean ("ok") trees, trees carrying
any warning, error or message, or (with \code{qsf_filter_flagged()}) anything
with a log entry at all. Each one still takes \code{...}, so it composes with
the other \code{qsf_filter()} criteria (\code{dbh}, \code{height}, ...) in a single
call. See details section for more in depth details
}
\details{
\itemize{
\item \code{qsf_filter_ok()} retains QSMs with no log entry at all. They are assumed to be
good QSMs.
\item \code{qsf_filter_sapling()} retains QSMs for which no measurements were performed
according to the height allometry rules (see \href{https://r-lidar.github.io/arbor_book/qsm.html#sec-tinytree}{Arbor Book}).
Under Arbor's definition, they are considered saplings.
\item \code{qsf_filter_nomeasure()} retains QSMs for which no measurements were found because
the QSM algorithm failed to measure anything. However, these trees are not saplings.
Saplings are different. For sapling the algortihm does not even try, on purpose.
\item \code{qsf_filter_broken()} retains QSMs for which Arbor automatically detected
that they correspond to broken trees.
\item \code{qsf_filter_flagged()} retains QSMs with at least one log entry, of any kind.
\item \code{qsf_filter_warnings()} retains QSMs carrying at least one warning.
\item \code{qsf_filter_errors()} retains QSMs carrying at least one error.
\item \code{qsf_filter_messages()} retains QSMs carrying at least one plain message.
}
}
\examples{
f <- system.file("extdata", "oak-plantation.qsf", package="arbor")
qsf <- qsf_read(f)

# qsf_filter_*(): quick selectors
qsf_filter_ok(qsf)               # trees with no log entry at all
qsf_filter_sapling(qsf)          # saplings according to arbor's definition
qsf_filter_flagged(qsf)          # trees with at least one log entry, any kind
qsf_filter_warnings(qsf)         # trees carrying at least one warning
qsf_filter_warnings(qsf, "W2")   # ... specifically warning code W2
qsf_filter_messages(qsf)         # trees carrying at least one plain message

# qsf_filter(): the general-purpose filter
# Keep only trees flagged with code W0 or W3
qsf_filter(qsf, code = c("W0", "W3"))

# Exclude trees flagged with code W2 or W3, while keeping trees with DBH > 20 cm
qsf_filter(qsf, code = c("W2", "W3"), invert = TRUE, dbh = c(0.2, Inf))

# Trees with no log entry at all ("good" QSMs)
qsf_filter(qsf, flag = FALSE)

# Trees with at least one log entry, of any kind
qsf_filter(qsf, flag = TRUE)

}
\seealso{
\link{qsf_log}
}
