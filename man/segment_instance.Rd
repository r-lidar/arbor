% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/segment_instance.R
\name{segment_instance}
\alias{segment_instance}
\title{Individual Tree Instance Segmentation}
\usage{
segment_instance(las, seeds, params)
}
\arguments{
\item{las}{A \code{LAS} object (from the \code{lidR} package) containing the point cloud data. It must have
an attribute \code{foliage}, which can be generated using prior semantic segmentation.}

\item{seeds}{An \code{sf} object containing points that represent tree seeds. Each point must have
xyz coordinates and a \code{treeID}. Seeds can be generated using \link{find_seeds}.}

\item{params}{list See \link{parameters}.}
}
\value{
A \code{LAS} object with an additional attribute \code{treeID}, indicating the ID of the tree to
which each point belongs.
}
\description{
This function performs individual tree instance segmentation by constructing a point-based network
and computing the least-cost path for each point to predefined seed points. The goal is to assign
each point in the point cloud a \code{treeID} corresponding to its nearest seed, based on the least-cost
path computed through the network. The process incorporates both spatial and heuristic constraints
to segment trees effectively. See the \href{placeholder}{Arbor book} for mode details.
}
\seealso{
\link{find_seeds}, \link{segment_semantic}
}
