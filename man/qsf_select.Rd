% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/qsf_filter.R
\name{qsf_select}
\alias{qsf_select}
\title{Filter a qsf object}
\usage{
qsf_select(
  qsf,
  code = NULL,
  type = NULL,
  invert = FALSE,
  dbh = NULL,
  max_height = NULL,
  ...
)
}
\arguments{
\item{qsf}{A `qsf` object.}

\item{code}{character. One or more \link{qsf_log} codes (e.g. `c("W0", "E1")`).
By default, trees carrying any of these codes are **kept**. Use `invert = TRUE`
to instead **exclude** trees carrying these codes.}

\item{type}{character. One or more log types: `"warning"`, `"message"`, `"error"`.
Same semantics as `code`, and combined with `code` (if both given, an entry
must match both to be considered flagged).}

\item{invert}{logical. If `FALSE` (default), trees matching `code`/`type` are
kept. If `TRUE`, matching trees are dropped.}

\item{dbh}{numeric(2). Keep only trees with a DBH (see \link{qsm_dbh}) inside
`c(min, max)`, in meters.}

\item{max_height}{numeric(2). Keep only trees with a height (see \link{qsm_height})
inside `c(min, max)`, in meters.}

\item{...}{Unused. Reserved for additional filters in the future.}
}
\value{
A `qsf` object containing only the selected QSMs.
}
\description{
Subsets a `qsf` object, retaining only the trees that satisfy the
requested criteria. Filters compose with AND semantics: a tree is retained
only if it satisfies every criterion supplied.
}
\examples{
f <- system.file("extdata", "oak-plantation.qsf", package="arbor")
qsf <- qsf_read(f)

# Keep only trees flagged with code W2
qsf_select(qsf, code = c("W0", "W3"))

# Exclude trees flagged with code W2 or W3, while keeping trees with DBH > 20 cm
qsf_select(qsf, code = c("W2", "W3"), invert = TRUE, dbh = c(0.2, Inf))
}
\seealso{
\link{qsf_log} \link{qsf_merchantable}
}
