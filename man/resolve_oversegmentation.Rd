% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/segment_overfitting.R
\name{resolve_oversegmentation}
\alias{resolve_oversegmentation}
\title{Resolve over-segmentation in a tree instance segmentation}
\usage{
resolve_oversegmentation(las)
}
\arguments{
\item{las}{A \code{LAS} object. Must already carry a \code{treeID}
attribute (from a prior instance segmentation) and a \code{HAG}
(height above ground) attribute, since detection only operates within
a configured HAG range.}
}
\value{
The input \code{LAS} object, with \code{treeID} values remapped
wherever over-segmentation was detected and merged. Instances that were
not found to be over-segmented are left untouched.
}
\description{
Detects and merges tree instances that were split into multiple treeIDs
during segmentation (over-segmentation) and merges them back into single,
consistent instances. This module is already executed by \code{\link{segment_instance}}.

Over-segmentation is detected by scanning the point cloud layer by layer
across a range of height-above-ground (HAG) values. Within each layer,
points are clustered (DBSCAN) and a circular stem cross-section is fitted
to each cluster. When a well-fitted circle's inlier points are supported
by more than one \code{treeID}, this is evidence that two (or more)
instances actually belong to the same stem. Evidence is accumulated
across all layers into a matching table of (reference, candidate) treeID
pairs, each backed by a count of how many circles support the pair and
how many inlier points came from each side. Chains of matches (e.g.
1-2, 2-3) are resolved with a union-find so that every id in a chain
collapses onto a single canonical id.
}
\seealso{
\code{\link{segment_instance}}
}
